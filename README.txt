README

Known Issues:

* Switching to Month view, then back to Agenda view, then pressing ADD (action Bar) then pressing CANCEL (action Bar)
  will cause the app to crash.
* Button and label sizes in Month view change size for different month names.
* Adding an event while in Month view will not automatically refresh the list view at the bottom
* Month view looks a bit crappy
* Ditto for Event Editor 

  

